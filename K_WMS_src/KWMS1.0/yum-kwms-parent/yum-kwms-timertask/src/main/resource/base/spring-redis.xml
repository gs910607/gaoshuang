<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd 
	http://www.springframework.org/schema/tx 
	http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
	http://www.springframework.org/schema/aop 
	http://www.springframework.org/schema/aop/spring-aop-4.0.xsd 
	http://www.springframework.org/schema/context 
	http://www.springframework.org/schema/context/spring-context-4.0.xsd"
	default-lazy-init="true" default-autowire="byName">

	<bean class="com.hoperun.emp.cache.redis.EmpJedisClient">
		<constructor-arg index="0" ref="jedisPoolConfig" />
		<property name="prefixStr" value="${redis.prefix}"></property>
		<property name="storeStyle" value="${redis.store.style}"></property>
	</bean>

	<bean id="jedisPoolConfig" class="com.hoperun.emp.cache.redis.JedisPoolConfig">
		<property name="poolType" value="${redis.pool.type}" />
		<property name="sentinelMasters" value="${redis.sentinel.masters}" />
		<property name="sentinelHosts" value="${redis.sentinel.hosts}" />
		<property name="sharedHost" value="${redis.shared.host}" />
		<property name="sharedPort" value="${redis.shared.port}" />
		<property name="maxTotal" value="${redis.pool.maxTotal}" />
		<property name="maxIdle" value="${redis.pool.maxIdle}" />
		<property name="maxWaitMillis" value="${redis.pool.maxWaitMillis}" />
		<property name="testOnBorrow" value="${redis.pool.testOnBorrow}" />
	</bean>


	<!-- ShardedSentinelJedisPool Setting Begin 
	<bean id="shardedSentinelJedisPool" class="com.hoperun.emp.cache.redis.ShardedJedisSentinelPool" >
		<constructor-arg index="0" ref="jedisPoolConfig" />
		<constructor-arg index="1" type="java.lang.String" value="${redis.sentinel.masters}"/>
		<constructor-arg index="2" type="java.lang.String" value="${redis.sentinel.hosts}"/>
	</bean>
	ShardedSentinelJedisPool Setting end -->
	
	<!-- ShardedJedisPool Setting Begin
<bean id="shardedJedisPool" class="redis.clients.jedis.ShardedJedisPool">
		<constructor-arg index="0" ref="jedisPoolConfig" />
		<constructor-arg index="1">
			<list>
				<ref bean="jedis.shardInfo" />
			</list>
		</constructor-arg>
	</bean>

	<bean id="jedis.shardInfo" class="redis.clients.jedis.JedisShardInfo">
		<constructor-arg index="0" value="${redis.shared.host}" />
		<constructor-arg index="1" value="${redis.shared.port}" type="int"/>
	</bean> -->
	<!-- ShardedJedisPool Setting End -->
	
	
	
	<!-- JedisPool Setting Begin -->
	<!-- <bean id="jedisPool" class="redis.clients.jedis.JedisPool">
		<constructor-arg index="0" ref="jedisPoolConfig"></constructor-arg>
		<constructor-arg index="1" type="java.lang.String" value="${}"></constructor-arg>
		<constructor-arg index="2" value="${}"></constructor-arg>
	</bean> -->
	<!-- JedisPool Setting End -->
	
</beans>
